# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Validation / env
python-dotenv==1.0.1
email-validator==2.1.1
python-multipart==0.0.9

# AI (optional — only needed for /explain endpoint)
openai==1.30.1

# Payments (optional — only needed for Razorpay endpoints)
razorpay==1.3.0
